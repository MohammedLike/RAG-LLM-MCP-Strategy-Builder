# Qdrant custom configuration
log_level: INFO

service:
  host: 0.0.0.0
  http_port: 6333
  grpc_port: 6334

storage:
  storage_path: /qdrant/storage
  on_disk_payload: true
  optimizers:
    default_segment_number: 2
